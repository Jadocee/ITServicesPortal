package com.spacejaam.itservicesportal.model.issue;

import com.spacejaam.itservicesportal.issue.Issue;

import java.time.LocalDateTime;
import java.util.List;
import java.util.ArrayList;
/*
 *
 *
public class Solution {
    //Will need a button in issue.jsp to connect with
    //will also need to pin at top of board.
        /*
        Maybe do this through looping through array after adding the solution to end of array and deleting it from
        original position then cycle through list adding current to the end of list and deleting

    void topComment(int i)
    {
        new Comment(comments[i].getID(), comments(i).getMessage(), comments(i).getCreated(), comments(i).getAuthor());
        comments(i).remove();
        i = 0;

        for(int o = 0; o < comments.size() - 1; o++)
        {
            comments(i) = new Comment(comments(i).getID(), comments(i).getMessage(), comments(i).getCreated(), comments(i).getAuthor());
            comments(i).remove();
        }
    }
}
*/